# 单元 D09-A：保留输入、分流拒收、自动验收

| 课程信息 | 内容 |
| --- | --- |
| 所属课程 | Data Warehousing with Apache Doris · Level 1 |
| 前置知识 | 完成 D05，了解导入响应与业务质量的区别 |
| 实验环境 | 课程独立实验库 |
| 目标版本 | Apache Doris 4.1.3；实际验证范围见[验证记录](../../../../maintenance/02-data-warehousing/VALIDATION.md) |
| 建议时间 | 约 40 分钟，含实验与测验，不含环境准备；待试讲校准 |

[课程目录](../README.md) · [打开实验 9A](lab9a_validate_orders.ipynb) · [打开测验 9A](quiz9a_data_quality_and_rejection.ipynb)

## 单元目标

导入后的记录里混有非法金额和缺失订单号，如何解释每一条输入的去向？本单元保留原始数据，显式分流合格与拒收记录，再验证质量检查确实能够发现错误。

## 学习目标

完成本单元后，你应该能够：

1. 区分类型校验与业务质量规则。
2. 保留输入标识和原始字段，解释拒收原因。
3. 核对全部输入都进入合格或拒收集合，且没有重复分流。
4. 结合逐行比较和错误注入检验质量规则。

## 单元安排

| 环节 | 学习形式 | 建议时间 | 学习成果 |
| --- | --- | --- | --- |
| D09-01：Schema 与业务质量 | 讲解与示例 | 5 分钟 | 定义 Schema 与业务检查边界 |
| D09-02：暂存与拒收 | 讲解与示例 | 5 分钟 | 按原始标识分流并追踪拒收原因 |
| D09-03：质量测试与验收 | 讲解与示例 | 5 分钟 | 证明质量规则能够发现错误 |
| 实验 9A | 动手操作 | 20 分钟 | 完成下方实验并核对结果 |
| 测验 9A | 五道知识测验 | 5 分钟 | 检查概念与场景选择 |

当前只覆盖样本声明的两类输入异常；Schema Change 时延、动态新鲜度及 dbt/调度集成尚待补充。

## D09-01：Schema 与业务质量

导入成功只能证明数据经过了这次导入规则。业务还关心主键完整性、金额、重复、及时性和状态逻辑。Schema 变化也需要检查下游兼容性。

质量规则应与数据契约一起版本化。新增字段与类型重写可能有不同代价，首版没有实现 Schema Change 时延实验，不宣称秒级变更已经验证。

**观察与练习：** 列出本样本的两种输入异常和五类检查，说明哪些在导入阶段发现、哪些必须在业务表检查。

## D09-02：暂存与拒收

要追踪错误输入，先保留输入行 ID 和原始字段，再显式分流。接口过滤掉的行不会自动进入我们设计的拒收表。

拒收原因与原始输入关联，校验集合需要覆盖全部输入且不交叉。原始行数和去重后的业务订单数是不同统计口径。

**观察与练习：** 12 行输入分为 10 行合格、2 行拒收；通过 input_id 回查错误字段。

## D09-03：质量测试与验收

使用独立预期结果逐行核对，再检查总量与业务规则。只检查行数可能漏掉一行丢失、另一行重复抵消的错误。

校验脚本还应通过故意制造错误来证明它会失败。固定截止时间用于离线业务口径，真实接入延迟必须另记录动态观测时间。

**观察与练习：** 注入一个重复订单，确认质量检查失败，然后恢复并再次通过。SQL 规则先跑通，dbt/调度集成后续再加。

## 动手实验 9A：保留输入、分流拒收、自动验收

打开[实验 9A](lab9a_validate_orders.ipynb)，按顺序完成：

1. 暂存十二行输入，包括非法金额与缺失订单号。
2. 显式分流为十行合格、两行拒收，按 input_id 回查原因。
3. 逐行核对合格记录，注入重复订单验证检查失败，再恢复正确数据。

完成后保存查询结果与差异原因；未执行的步骤不要标记为完成。

### 数据来源与说明

实验使用仓库自带的合成订单及变更样本，不含真实客户数据。
字段、业务口径和预期结果见[数据说明](../../datasets/README.md)。

## 单元总结

- 被接口过滤的行不会自动进入业务拒收表。
- 保留原始输入和稳定标识，才能解释校验与分流结果。
- 总量正确不足以证明明细正确；质量规则需要用已知错误验证。

## 知识测验 9A：保留输入、分流拒收、自动验收

完成讲义和实验后，打开[测验 9A](quiz9a_data_quality_and_rejection.ipynb)。
测验包含五道单选题，不依赖 Doris 或外部服务；提交后阅读答案解释。
能解释结果和选择原因，比只记住命令名称更重要。

## 官方参考资料

- [Stream Load：参数、返回结果与导入示例](https://doris.apache.org/docs/4.x/data-operate/import/import-way/stream-load-manual/)
- [Schema Change：表结构变更](https://doris.apache.org/docs/4.x/table-design/schema-change/)

官方文档会随版本更新；实验中的目标版本和实际验证范围以课程信息为准。
